DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/yourdb
DEEPSEEK_API_KEY=sk-...
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_EMBEDDING_MODEL=deepseek-embedding
VOYAGE_API_KEY=voyage-...
VOYAGE_MODEL=voyage-3.5
ENABLE_OCR=true # `true` to allow OCR fallback; set `false` to reject scanned PDFs.
TESSERACT_LANGS=amh+eng # to match installed language data (default `amh+eng`).